# LogicX-Push2-SplitView (macOS) – top-level deps only.
# Lock with pip-tools -> requirements.txt

# Your Push2 library (pin to a known-good commit for reproducible installs)
push2-python @ git+https://github.com/zurie/push2-python@ae91501788469f2c54aa95ed12f37f83505d73f8

# Web UI / Socket.IO
flask>=3.0,<4
flask-socketio>=5.3,<6
eventlet>=0.35,<1

# MIDI / OSC / USB
mido>=1.3,<2
python-rtmidi>=1.5,<2
oscpy>=0.6,<1
pyusb>=1.2,<2

# Rendering / images
cairocffi>=1.7,<2
pillow>=10,<13

# Utilities
psutil>=5.9,<8

# Keystrokes on macOS (Logic control)
pynput>=1.7,<2

# Keep NumPy on 1.x to avoid NumPy 2.x migration surprises unless you need 2.x
numpy>=1.26,<2
